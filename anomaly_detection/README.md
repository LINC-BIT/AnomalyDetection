# General anomaly-detection layer

Shared anomaly-detection interfaces do not assume textile input. A trained memory bank, threshold, normalization statistic, prompt adapter, or checkpoint remains domain-specific even when its algorithm is reusable. Add a dataset adapter and separately trained manifest entry for each new material domain; never silently reuse textile weights.
