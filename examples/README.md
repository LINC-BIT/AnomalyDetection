# Examples

Run examples from the repository root after installing the editable package. `python examples/01_inspect_catalog.py`, `adh inventory`, and `adh doctor` require no downloads and form the recommended Minimal Working Example (MWE).
