# Textile dataset inventory

This directory contains locally available fabric datasets. Original directory names are preserved because adapters and recipes use them as stable defaults. After adding or replacing data, run `adh doctor` from the repository root. Dataset bytes are local runtime assets and must not be committed.
